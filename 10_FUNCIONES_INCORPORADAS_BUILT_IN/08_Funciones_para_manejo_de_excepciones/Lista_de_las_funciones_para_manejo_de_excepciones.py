# Lista_de_las_funciones_para_manejo_de_excepciones.py

# Código:
Lista_de_las_funciones_incorporadas_y_palabras_clave_para_manejo_de_excepciones = [

    # Palabras clave para el manejo de excepciones.
    "try",           # Bloque para gestionar excepciones.
    "except",        # Bloque para capturar excepciones.
    "finally",       # Bloque que se ejecuta siempre, independientemente de si se produjo una excepción o no.
    "raise",         # Lanza una excepción.
    "assert",        # Verifica una condición y lanza una excepción si es falsa.

    # Funciones incorporadas útiles para el manejo de excepciones.
    "isinstance()",  # Verifica si un objeto es una instancia de una clase.
    "issubclass()",  # Verifica si una clase es una subclase de otra.
    "locals()",      # Devuelve un diccionario de variables locales (útil en depuración).
    "globals()",     # Devuelve un diccionario de variables globales (útil en depuración).
    "vars()",        # Devuelve el atributo "__dict__" de un objeto (útil en depuración).
]

# ════════════════════════════════════════════════════════════
#  Código corregido, revisado y optimizado por el autor.
#  Autor: Joe De Jesús Fernández Diniz.
# ────────────────────────────────────────────────────────────
