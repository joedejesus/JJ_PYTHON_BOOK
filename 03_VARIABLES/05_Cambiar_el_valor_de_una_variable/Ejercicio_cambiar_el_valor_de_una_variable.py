# Enunciado:
"""Define una variable llamada "x" con un valor inicial de 10. Luego, cambia el valor de "x" a 15  y
muestra el resultado. Después, define otra variable llamada "z" con un valor inicial de 156.  Luego,
cambia el valor de "z" a 985 y guarda ese resultado en la variable "resultado". Finalmente,  imprime
el valor de la variable "resultado" en la consola utilizando la función "print()"."""

# Ejercicio_cambiar_el_valor_de_una_variable.py

# Explicación:
"""Definimos una variable llamada "x" y le asignamos un valor inicial de  10.  Luego,  cambiamos  el
valor de "x" a 15. Finalmente, mostramos el nuevo valor de "x" en la consola utilizando  la  función
"print()". En este caso, el valor inicial de "x" se sobrescribe con  el  nuevo  valor,  y  el  valor
inicial se pierde."""

# Código:
x = 10    # Valor inicial de "x".
x = 15    # Nuevo valor de "x".
print(x)  # Mostramos el nuevo valor de "x" en la consola.

# Explicación:
"""Definimos una variable llamada "z" y le asignamos un valor inicial de 156.  Luego,  cambiamos  el
valor de "z" a 985. Después, guardamos el nuevo valor de la variable "z" en la variable  "resultado"
al asignárselo. En este caso, el valor inicial de "z" se sobrescribe con el nuevo valor y se pierde,
pero el nuevo valor se guarda en  la  variable  "resultado",  que  puede  utilizarse  más  adelante.
Finalmente, mostramos el valor de la variable  "resultado"  en  la  consola  utilizando  la  función
"print()"."""

# Código:
z = 156           # Valor inicial de "z".
z = 985           # Nuevo valor de "z".
resultado = z     # Guardamos el nuevo valor de "z" en la variable "resultado".
# Mostramos el valor de la variable "resultado" con el valor modificado de "z" en la consola.
print(resultado)

# Nota Muy Importante:
"""El valor inicial de "z" podría guardarse en otra variable antes de cambiarlo, pero en  este  caso
no es necesario. Es importante resaltar la diferencia entre cambiar  el  valor  de  una  variable  y
cambiar el tipo de dato de una variable. En este caso, se cambia el valor de la variable, no su tipo
de dato. En la sección de manejo de tipos de datos se explica cómo cambiar el tipo de  dato  de  una
variable."""

# ════════════════════════════════════════════════════════════
#  Código corregido, revisado y optimizado por el autor.
#  Autor: Joe De Jesús Fernández Diniz.
# ────────────────────────────────────────────────────────────